<!-- pages/index/index.wxml -->
<view>
  <view class="recommendation" wx:for="{{recommendations}}" wx:key="unique">
    <text>{{item.name}}</text> <!-- Example of a recommendation item -->
    <text>{{item.description}}</text> <!-- Example of a description -->
  </view>
</view>
