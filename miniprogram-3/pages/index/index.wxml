<!-- pages/index/index.wxml -->
<view>
  <text>Attractions:</text>
  <block wx:for="{{attractions}}" wx:key="id">
    <view>{{item.name}}</view>  <!-- Display the name of each attraction -->
  </block>
</view>
