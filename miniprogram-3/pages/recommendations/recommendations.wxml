<!-- pages/recommendations/recommendations.wxml -->
<view>
  <text>Recommendations:</text>
  <block wx:for="{{recommendations}}" wx:key="id">
    <view>{{item.name}}</view>  <!-- Display the name of each recommendation -->
  </block>
</view>
